# Copie para .dev.vars e preencha, pra rodar `npm run preview` (build +
# `wrangler dev`, simulando o runtime real do Cloudflare Workers em vez do
# `next dev` normal). Mesmos valores do .env.example/.env.local.
DATABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
